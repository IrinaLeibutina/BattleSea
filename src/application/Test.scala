package application
import scala.collection.immutable.ListMap
import scala.collection.immutable.ListMap
import scala.collection.immutable.SortedMap

object Test {

val m = Map( 2->("Two":Any), 1 -> ("one":Any))
val sorted = SortedMap[Int, Any]() ++ m
 // def sum(a: Int, b: Int): Int = {
 //   a + b    
//  }

 
}